// Script 228
#include "Fomt_Lib.src"

script 228 Event_0228 {
    const MESSAGE_0 = "The wind is too strong\r\nto go outside.\BRK";
    const MESSAGE_1 = "The snow is too deep\r\nto go outside.\BRK";
    
    if ((GetEntityFacing(0) == 0)) {
        if ((Check_Flag(6) == 3)) {
            TalkOpen();
            TalkMessage(0);
            TalkClose();
            SetEntityFacing(0, "Up");
            if ((Inventory_Check() == 1)) {
                SetEntityAnim(0, 0x1C6);
            } else {
                SetEntityAnim(0, 0x156);
            }
            Set_Vector_Y(0, 0xC8, 1);
            Execute_Movement(0);
            exit;
        }
        if ((Check_Flag(6) == 4)) {
            TalkOpen();
            TalkMessage(1);
            TalkClose();
            SetEntityFacing(0, "Up");
            if ((Inventory_Check() == 1)) {
                SetEntityAnim(0, 0x1C6);
            } else {
                SetEntityAnim(0, 0x156);
            }
            Set_Vector_Y(0, 0xC8, 1);
            Execute_Movement(0);
            exit;
        }
        Fade_Out(2, 1);
        Warp_Player(0, 0x168, 0x90);
        SetEntityPosition(0, 0x168, 0x90, "Down");
        PlaySong(0, 0xCD);
        Fade_In(2, 1);
    }
    exit;
}
